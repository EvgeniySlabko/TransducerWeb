import { saveAs } from 'file-saver'
import { serialize } from 'serialize-ts';
import { Snapshot } from '../ReportListener/Snapshot';
var FileSaver = require('file-saver');

export function saveStaticDataToFile(snapshot: Snapshot) {
    
}