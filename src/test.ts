require('./css/styles.css');
require('../js/chart');
import RingBuffer  from "./Buffer"

//import a from "./tss"

const person: any = {};
console.log(person.speak());
var a = new RingBuffer(5);
console.log(a.size);